$(document).ready(function(){
	//pingfore_request_server();
});

function pingfore_request_server()
{
	//$.get('index.php?mod=pingfore', function (data){
	//	var next = parseInt(data);
	//	if (isNaN(next))
	//	{
	//		next = 10;
	//	}
	//	setTimeout(pingfore_request_server, next * 1000);
	//});
}